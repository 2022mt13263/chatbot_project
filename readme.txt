create a project with name  "chatbot_project"
install react 
once project is created,copy the code of "frontend/src" folder into react's file (app.js and Css files)
install node.js
once project is created,copy the code of "backend/src" folder into backend's files (index.js)

open terminal
run npm start to run frontend
open 1 more terminal
run node index command on terminal

open "http://localhost:3000/"
 